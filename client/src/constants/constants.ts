enum Keys {
    Shift = 'Shift',
}

enum Mouse {
    LeftButton = 0,
    MouseWheel = 1,
    RightButton = 2,
}

const SIDEBAR_WIDTH = 360;

const SVG_NS = 'http://www.w3.org/2000/svg';

export { SIDEBAR_WIDTH, SVG_NS, Keys, Mouse };
